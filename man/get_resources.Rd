% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resources.R
\name{get_resources}
\alias{get_resources}
\title{Get Resources from Eratos}
\usage{
get_resources(
  query = NULL,
  limit = 20,
  skip = 0,
  exclude = NULL,
  type = NULL,
  excludeType = NULL,
  owner = NULL,
  collections = NULL,
  facets = NULL,
  sort = "@date:asc",
  scope = NULL,
  property_filters = NULL
)
}
\arguments{
\item{query}{Character string. Free text search query - supports wildcards (*) and simple AND/OR logic.}

\item{limit}{Integer (1-100). Limit the number of results. Defaults to 20.}

\item{skip}{Integer (>= 0). Starting index for results. Defaults to 0.}

\item{exclude}{Character string. Comma separated list of ERNs or IDs to exclude.}

\item{type}{Character string. Comma separated list of ERNs or IDs of schema types to include.}

\item{excludeType}{Character string. Comma separated list of ERNs or IDs of schema types to exclude.}

\item{owner}{Character string. Comma separated list of owner ERNs.}

\item{collections}{Character string. Comma separated list of collection ERNs.}

\item{facets}{Character string. Comma separated list of facets.}

\item{sort}{Character string. Sort order. Defaults to "@date:asc". 
If sorting other than name or @date, type must be specified.}

\item{scope}{Character string. Scope of the query (e.g. "Public" or "Private").}

\item{property_filters}{Named list. Advanced property filtering in the form 
list(property_name = list(op = "eq", value = "search_value")). 
Supported operations: "eq" (equal), "ne" (not equal), "gt" (greater than), 
"lt" (less than), "ge" (greater than or equal), "le" (less than or equal).
Note: Property functions require type to also be set.}
}
\value{
A list containing the API response with resource data.
}
\description{
Find resources using the Eratos workspace API with various search and filter options.
}
\details{
This function queries the Eratos workspace API to find resources matching the 
specified criteria. The API supports both simple text search and advanced 
property-based filtering.

For advanced querying using property_filters, only properties that exist in 
the schema type definition can be queried, and not all properties within a 
schema are queryable.
}
\examples{
\dontrun{
# Simple text search
resources <- get_resources(query = "temperature*")

# Limit results and skip first 10
resources <- get_resources(query = "sensor", limit = 50, skip = 10)

# Filter by type and owner
resources <- get_resources(
  type = "ern:e-pn.io:resource:eratos.sensor", 
  owner = "ern:e-pn.io:resource:eratos.user.john"
)

# Advanced property filtering
resources <- get_resources(
  type = "block",
  property_filters = list(
    creator = list(op = "eq", value = "ern:e-pn.io:resource:eratos.creator.eratos")
  )
)

# Sort by date descending
resources <- get_resources(sort = "@date:desc")
}

}
